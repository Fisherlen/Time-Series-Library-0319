# Time-Series-Library 数据集安装说明

## 数据集存储位置

所有数据集已成功下载到以下目录：

```
l:\AI\Trae\Time-Series-Library-0319-Doubao\dataset
```

## 数据集目录结构

```
dataset/
├── MSL/                    # MSL 异常检测数据集 (npy格式)
│   ├── MSL_train.npy
│   ├── MSL_test.npy
│   └── MSL_test_label.npy
├── PSM/                    # PSM 异常检测数据集 (csv格式)
│   ├── train.csv
│   ├── test.csv
│   └── test_label.csv
├── SMAP/                   # SMAP 异常检测数据集 (npy格式)
│   ├── SMAP_train.npy
│   ├── SMAP_test.npy
│   └── SMAP_test_label.npy
├── SMD/                    # SMD 异常检测数据集 (npy格式)
│   ├── SMD_train.npy
│   ├── SMD_test.npy
│   └── SMD_test_label.npy
├── SWAT/                   # SWAT 异常检测数据集 (csv格式)
│   ├── swat_train2.csv
│   └── swat2.csv
├── ETTh1.csv               # ETT 小时粒度数据集1
├── ETTh2.csv               # ETT 小时粒度数据集2
├── ETTm1.csv               # ETT 分钟粒度数据集1
├── ETTm2.csv               # ETT 分钟粒度数据集2
├── exchange_rate.csv       # 汇率数据集
├── national_illness.csv    # 国家疾病数据集
├── traffic.csv             # 交通流量数据集
└── weather.csv             # 天气数据集
```

## 数据集详情

| 数据集 | 类型 | 格式 | 训练集形状 | 测试集形状 |
|--------|------|------|------------|------------|
| ETTh1 | 预测 | CSV | (17420, 8) | - |
| ETTh2 | 预测 | CSV | (17420, 8) | - |
| ETTm1 | 预测 | CSV | (69680, 8) | - |
| ETTm2 | 预测 | CSV | (69680, 8) | - |
| exchange_rate | 预测 | CSV | (7588, 9) | - |
| national_illness | 预测 | CSV | (966, 8) | - |
| traffic | 预测 | CSV | (17544, 863) | - |
| weather | 预测 | CSV | (52696, 22) | - |
| PSM | 异常检测 | CSV | (132481, 26) | (87841, 26) |
| SWAT | 异常检测 | CSV | (495000, 52) | (449919, 52) |
| MSL | 异常检测 | NPY | (58317, 55) | (73729, 55) |
| SMAP | 异常检测 | NPY | (135183, 25) | (427617, 25) |
| SMD | 异常检测 | NPY | (708405, 38) | (708420, 38) |

## 使用方法

项目中的 `data_provider/data_loader.py` 已支持从本地加载数据集。当本地文件不存在时，代码会自动从 Hugging Face 下载。

### 示例：加载 ETTh1 数据集

```python
from data_provider.data_loader import Dataset_ETT_hour
import argparse

args = argparse.Namespace(augmentation_ratio=0)
dataset = Dataset_ETT_hour(
    args,
    root_path=r"l:\AI\Trae\Time-Series-Library-0319-Doubao\dataset",
    flag='train',
    size=[96, 48, 96],  # [seq_len, label_len, pred_len]
    features='M',
    data_path='ETTh1.csv',
    target='OT',
    scale=True,
    timeenc=0,
    freq='h'
)
```

### 示例：加载 MSL 异常检测数据集

```python
from data_provider.data_loader import MSLSegLoader
import argparse

args = argparse.Namespace()
dataset = MSLSegLoader(
    args,
    root_path=r"l:\AI\Trae\Time-Series-Library-0319-Doubao\dataset\MSL",
    win_size=100,
    step=1,
    flag="train"
)
```

## 相关脚本

- `download_datasets.py` - 完整的数据集下载脚本
- `download_missing.py` - 缺失数据集补充下载脚本
- `test_datasets.py` - 数据集加载测试脚本

## 注意事项

1. 数据集总大小约为 500MB
2. 所有数据集已验证可以正常加载
3. 项目运行时会优先使用本地数据集，如不存在则自动下载
4. 使用时请确保 `root_path` 参数指向正确的数据集目录
